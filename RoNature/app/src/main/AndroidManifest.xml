<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplicationandoroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationAndoroid">
        <activity
            android:name=".profil.SavedPostsActivity"
            android:exported="false" />
        <activity
            android:name=".users.UsersProfilActivity"
            android:exported="false" />
        <activity android:name=".home.PostDetailsActivity" />
        <activity android:name=".control.DashboardActivity" />
        <activity android:name=".authentification.LoginActivity" />
        <activity android:name=".authentification.RegistrationActivity" />
        <activity android:name=".profil.EditProfilePageActivity" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".home.PostLikedByActivity" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>